extends ../layout

block content
    h1 #{article.title}
    h4 Written By, #{article.author}
    p #{article.body}
    a.btn.btn-light(href="/article/edit/"+article._id) Edit
    a.btn.btn-danger.delete-article(href="javascript:void(0);", data-id=article._id) Delete